# Week 2 Day 1 基线性能测试执行指南

**日期**: 2025-10-23  
**状态**: 📝 测试流程演示（环境问题待修复后执行）

---

## ⚠️  当前环境状态

### 已完成
- ✅ Flyway baseline 配置已添加到 `docker-compose.yml`
- ✅ JMeter 测试计划已创建（`jvm-tuning-test.jmx`）
- ✅ 测试脚本已就绪（`run-test.ps1`, `quick-verify.ps1`）
- ✅ 监控配置已完成（JFR + Prometheus）

### 待解决
- ⏳ Docker 服务启动中（预计需要 2-3 分钟）
- ⏳ 等待所有微服务健康检查通过

---

## 📋 完整测试执行流程（演示）

### 阶段 1: 环境验证（5 分钟）

#### 1.1 检查 Docker 服务状态
```powershell
# 查看所有服务状态
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 预期输出: 所有 evcs-* 服务显示 "healthy"
```

#### 1.2 快速连通性测试
```powershell
cd performance-tests

# 执行快速验证（25 个请求）
.\quick-verify.ps1
```

**预期结果**:
```
=== 验证完成 ===

总请求数: 25
成功请求: 25  ✅
失败请求: 0

✅ 环境验证成功！可以执行完整性能测试
```

---

### 阶段 2: 基线性能测试（25 分钟）

#### 2.1 启动性能测试
```powershell
# 执行完整测试（3 个场景并行）
.\run-test.ps1

# 或单独执行场景
.\run-test.ps1 -Scenario scenario1  # 仅订单创建
.\run-test.ps1 -Scenario scenario2  # 仅订单查询
.\run-test.ps1 -Scenario scenario3  # 仅状态更新
```

#### 2.2 测试进度监控
**终端输出示例**:
```
=== 开始执行测试 ===
执行命令: jmeter -n -t jvm-tuning-test.jmx ...

Creating summariser <summary>
Created the tree successfully using jvm-tuning-test.jmx
Starting standalone test @ 2025 Oct 28 09:00:00 CST

summary +   5000 in 00:00:10 =  500.0/s Avg:   120 Min:    45 Max:   450 Err:     0 (0.00%) Active: 100
summary +  10000 in 00:00:20 =  500.0/s Avg:   115 Min:    42 Max:   380 Err:     0 (0.00%) Active: 100
...
summary = 300000 in 00:10:00 =  500.0/s Avg:   118 Min:    40 Max:   520 Err:     5 (0.00%)

Tidying up ...
... end of run
```

#### 2.3 自动生成报告
测试完成后自动生成并打开 HTML 报告：
```
performance-tests/results/report-20251028-090000/index.html
```

---

### 阶段 3: 结果分析（2 小时）

#### 3.1 HTML 报告分析

**Dashboard 关键指标**:

| 指标 | 场景1 (订单创建) | 场景2 (订单查询) | 场景3 (状态更新) |
|------|-----------------|-----------------|-----------------|
| **总请求数** | 300,000 | 600,000 | 600,000 |
| **成功率** | 99.95% | 99.98% | 99.90% |
| **错误率** | 0.05% | 0.02% | 0.10% |
| **响应时间 P50** | 95ms | 45ms | 38ms |
| **响应时间 P90** | 185ms | 92ms | 78ms |
| **响应时间 P95** | 245ms | 125ms | 105ms |
| **响应时间 P99** | 420ms ⚠️ | 198ms ✅ | 165ms ✅ |
| **平均 TPS** | 498 | 998 | 1995 |

**问题识别**:
- ⚠️  场景1 P99 响应时间 420ms，接近目标值 500ms
- ⚠️  场景3 错误率 0.10%，略高于目标 < 0.05%
- ✅ 场景2 性能表现优秀

**Response Times Over Time 趋势**:
- 📈 场景1: 在第 5-7 分钟出现响应时间峰值（GC？）
- 📊 场景2: 响应时间稳定，无明显波动
- 📉 场景3: 前 2 分钟响应时间较高（JVM 预热）

---

#### 3.2 JFR (Java Flight Recorder) 分析

##### 导出 JFR 文件
```powershell
# 从容器导出 JFR 文件
docker cp evcs-order:/app/logs/flight.jfr logs/jfr/baseline-order.jfr
docker cp evcs-gateway:/app/logs/flight.jfr logs/jfr/baseline-gateway.jfr
docker cp evcs-station:/app/logs/flight.jfr logs/jfr/baseline-station.jfr
```

##### 使用 JMC 打开分析
```powershell
# 启动 JDK Mission Control
jmc logs/jfr/baseline-order.jfr
```

**关键分析页面**:

1. **Memory** - 内存使用分析
   ```
   堆内存配置: 256MB - 512MB
   
   老年代使用率:
   - 平均: 68%
   - 峰值: 92% ⚠️  (接近触发 Full GC)
   
   新生代使用率:
   - 平均: 45%
   - 峰值: 78%
   ```

2. **Garbage Collections** - GC 事件分析
   ```
   Young GC:
   - 频率: 每 15 秒一次
   - 平均暂停: 25ms ✅
   - 最大暂停: 78ms ✅
   
   Full GC:
   - 次数: 测试期间 3 次 ⚠️
   - 平均暂停: 185ms ⚠️
   - 最大暂停: 245ms ⚠️  (超出目标 <200ms)
   
   总 GC 时间: 8.2 秒 (1.37% of runtime)
   ```

3. **Hot Methods** - 热点方法
   ```
   Top 5 CPU 消耗方法:
   1. com.evcs.order.service.impl.ChargingOrderServiceImpl.pageOrders - 18.5%
   2. org.springframework.data.redis.core.RedisTemplate.opsForValue - 12.3%
   3. com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper - 9.7%
   4. com.zaxxer.hikari.pool.HikariPool.getConnection - 7.2%
   5. java.util.concurrent.ThreadPoolExecutor.runWorker - 6.8%
   ```

4. **Lock Instances** - 锁竞争
   ```
   高竞争锁:
   - HikariPool.getConnection: 平均等待 12ms ⚠️
   - RedisConnectionFactory: 平均等待 8ms
   ```

---

#### 3.3 Prometheus 指标查询

访问 `http://localhost:9090` 并执行以下查询：

##### GC 指标
```promql
# GC 暂停时间（秒）
jvm_gc_pause_seconds_sum{service="evcs-order"}

# GC 暂停次数
jvm_gc_pause_seconds_count{service="evcs-order"}

# 平均暂停时间 = sum / count
jvm_gc_pause_seconds_sum / jvm_gc_pause_seconds_count
```

**结果示例**:
```
Young GC 平均暂停: 0.025s (25ms) ✅
Full GC 平均暂停: 0.185s (185ms) ⚠️
```

##### 内存指标
```promql
# 堆内存使用（字节）
jvm_memory_used_bytes{area="heap",service="evcs-order"}

# 堆内存使用率
jvm_memory_used_bytes{area="heap"} / jvm_memory_max_bytes{area="heap"} * 100
```

**结果示例**:
```
平均堆使用率: 68%
峰值堆使用率: 92% ⚠️  (触发 Full GC 临界值)
```

##### 连接池指标
```promql
# HikariCP 活跃连接数
hikaricp_connections_active{pool="evcs-order"}

# HikariCP 连接池使用率
hikaricp_connections_active / hikaricp_connections_max * 100
```

**结果示例**:
```
平均连接使用: 8 / 10 = 80%
峰值连接使用: 10 / 10 = 100% ⚠️  (连接池耗尽)
```

---

### 阶段 4: 编写基线报告（1 小时）

创建 `docs/week2-day1-baseline-report.md`:

````markdown
# Week 2 Day 1 - JVM 性能基线测试报告

**测试日期**: 2025-10-28  
**测试人员**: Performance Team  
**测试环境**: Docker Compose (本地)

---

## 1. 测试配置

### 1.1 JVM 配置
```yaml
JAVA_OPTS: >
  -Xms256m -Xmx512m
  -XX:+UseG1GC  # 默认 G1GC
  -XX:StartFlightRecording=dumponexit=true
```

### 1.2 硬件环境
- CPU: 16 核
- 内存: 32GB
- 磁盘: SSD

### 1.3 测试场景
| 场景 | 目标 TPS | 并发用户 | 持续时间 |
|------|----------|----------|----------|
| 订单创建 | 500 | 100 | 10 分钟 |
| 订单查询 | 1000 | 200 | 10 分钟 |
| 状态更新 | 2000 | 500 | 5 分钟 |

---

## 2. 性能指标汇总

### 2.1 响应时间

| 场景 | P50 | P90 | P95 | P99 | 目标 | 达标 |
|------|-----|-----|-----|-----|------|------|
| 订单创建 | 95ms | 185ms | 245ms | **420ms** | <500ms | ⚠️  临界 |
| 订单查询 | 45ms | 92ms | 125ms | **198ms** | <500ms | ✅ |
| 状态更新 | 38ms | 78ms | 105ms | **165ms** | <500ms | ✅ |

### 2.2 吞吐量

| 场景 | 目标 TPS | 实际 TPS | 达标率 |
|------|----------|----------|--------|
| 订单创建 | 500 | 498 | 99.6% ✅ |
| 订单查询 | 1000 | 998 | 99.8% ✅ |
| 状态更新 | 2000 | 1995 | 99.8% ✅ |

### 2.3 错误率

| 场景 | 错误率 | 目标 | 达标 |
|------|--------|------|------|
| 订单创建 | 0.05% | <1% | ✅ |
| 订单查询 | 0.02% | <1% | ✅ |
| 状态更新 | 0.10% | <1% | ✅ |

---

## 3. GC 分析

### 3.1 GC 统计

| GC 类型 | 次数 | 平均暂停 | 最大暂停 | 目标 | 达标 |
|---------|------|----------|----------|------|------|
| Young GC | 40 | 25ms | 78ms | <100ms | ✅ |
| Full GC | **3** | **185ms** | **245ms** | <100ms | ❌ |

### 3.2 GC 问题分析

**问题 1: Full GC 暂停时间过长**
- 现状: 平均 185ms，最大 245ms
- 目标: < 100ms
- 影响: 导致场景1 P99 响应时间接近 500ms

**问题 2: Full GC 频率过高**
- 现状: 测试期间 3 次 Full GC
- 原因: 老年代使用率峰值达 92%
- 建议: 增加堆内存或优化对象分配

---

## 4. 性能瓶颈识别

### 4.1 CPU 热点方法
1. `ChargingOrderServiceImpl.pageOrders` - 18.5%
   - 问题: 分页查询逻辑复杂
   - 优化方向: 添加 Redis 缓存

2. `RedisTemplate.opsForValue` - 12.3%
   - 问题: Redis 序列化开销大
   - 优化方向: 使用二进制序列化

3. `HikariPool.getConnection` - 7.2%
   - 问题: 连接池获取等待时间长
   - 优化方向: 增加连接池大小

### 4.2 锁竞争
- `HikariPool.getConnection`: 平均等待 12ms ⚠️
- `RedisConnectionFactory`: 平均等待 8ms

---

## 5. 优化建议（Day 2-3 执行）

### 5.1 GC 参数优化（高优先级）

#### 方案 1: G1GC 调优 - 减少停顿时间
```bash
JAVA_OPTS: >
  -Xms512m -Xmx1024m
  -XX:+UseG1GC
  -XX:MaxGCPauseMillis=50
  -XX:G1HeapRegionSize=4m
  -XX:InitiatingHeapOccupancyPercent=45
```

**预期效果**:
- Full GC 暂停时间: 185ms → **< 80ms**
- Full GC 次数: 3 次 → **0-1 次**

#### 方案 2: G1GC 调优 - 增加吞吐量
```bash
JAVA_OPTS: >
  -Xms512m -Xmx1024m
  -XX:+UseG1GC
  -XX:MaxGCPauseMillis=100
  -XX:G1HeapRegionSize=8m
  -XX:ParallelGCThreads=8
```

**预期效果**:
- 吞吐量提升: **+10%**
- GC 开销: 1.37% → **< 1%**

#### 方案 3: ZGC 测试 - 超低延迟
```bash
JAVA_OPTS: >
  -Xms512m -Xmx1024m
  -XX:+UseZGC
  -XX:ZAllocationSpikeTolerance=5
```

**预期效果**:
- GC 暂停时间: **< 10ms** (亚毫秒级)
- 响应时间 P99: **< 300ms**

### 5.2 连接池优化（中优先级）

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20  # 当前: 10
      minimum-idle: 10       # 当前: 5
      connection-timeout: 20000
```

**预期效果**:
- 连接等待时间: 12ms → **< 5ms**
- 连接池使用率: 100% → **60-80%**

---

## 6. 下一步行动

### Day 2（2025-10-29）
- [ ] 实施 GC 方案 1 并测试
- [ ] 实施 GC 方案 2 并测试
- [ ] 对比分析选择最优配置

### Day 3（2025-10-30）
- [ ] 实施 ZGC 方案并测试
- [ ] 对比所有方案性能
- [ ] 确定最终 GC 配置

### Day 4（2025-10-31）
- [ ] 实施连接池优化
- [ ] 执行回归测试
- [ ] 验证优化效果

---

## 7. 附件

- HTML 报告: `performance-tests/results/report-20251028-090000/index.html`
- JFR 文件: `logs/jfr/baseline-*.jfr`
- Prometheus 数据: `http://localhost:9090`

---

**报告生成时间**: 2025-10-28 14:00  
**审核人**: Tech Lead
````

---

## 📊 优化目标对比

| 指标 | 基线 | Week 2 目标 | 改进幅度 |
|------|------|-------------|----------|
| GC 暂停时间 (Full GC) | 185ms | < 100ms | **-46%** |
| 响应时间 P99 | 420ms | < 300ms | **-29%** |
| 吞吐量 | 基线 | +10% | **+10%** |
| 连接池使用率 | 100% | 60-80% | 优化资源利用 |

---

## ✅ 测试完成检查清单

**数据收集**:
- [ ] JMeter HTML 报告已生成
- [ ] JFR 文件已导出
- [ ] Prometheus 指标已记录
- [ ] 关键截图已保存

**分析完成**:
- [ ] 响应时间趋势已分析
- [ ] GC 行为已识别
- [ ] 性能瓶颈已定位
- [ ] 优化方案已制定

**文档交付**:
- [ ] 基线报告已编写
- [ ] 优化计划已更新
- [ ] Git 提交已完成

---

**当前状态**: 📝 等待环境就绪后执行实际测试

**预计完成时间**: 2025-10-28 (Week 2 Day 1)
