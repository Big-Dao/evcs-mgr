# 租户服务配置文件
# 主要配置在config-repo/evcs-tenant-local.yml中
# 本地文件仅保留Config Server连接配置
spring:
  application:
    name: evcs-tenant
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
  cloud:
    config:
      profile: local
